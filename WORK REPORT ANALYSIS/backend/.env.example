# Django
SECRET_KEY=change-me-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (PostgreSQL)
DB_NAME=work_report_db
DB_USER=work_report_user
DB_PASSWORD=work_report_pass
DB_HOST=127.0.0.1
DB_PORT=5432

# Google OAuth — paste your OAuth 2.0 Client ID here (same value as frontend VITE_GOOGLE_CLIENT_ID)
GOOGLE_OAUTH_CLIENT_ID=

# CORS / frontend origin
FRONTEND_ORIGIN=http://localhost:5173
